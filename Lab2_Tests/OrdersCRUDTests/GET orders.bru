meta {
  name: GET orders
  type: http
  seq: 2
}

get {
  url: {{baseURL}}/api/orders
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer lab2_secret_key
}

assert {
  res.body.length: eq 1
}

settings {
  encodeUrl: true
  timeout: 0
}
